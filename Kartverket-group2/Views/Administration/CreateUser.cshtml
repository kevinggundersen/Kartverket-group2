@* View for creating a new user with roles assignment *@

@model CreateUserViewModel

@{
    ViewData["Title"] = "Create User";
}

<h2>Create User</h2>

@* User creation form with email, username, password fields and role selection *@
<form asp-action="CreateUser" method="post">
    @* Email input with validation *@
    <div class="form-group">
        <label asp-for="Email"></label>
        <input asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>

    @* Username input with validation *@
    <div class="form-group">
        <label asp-for="Username"></label>
        <input asp-for="Username" class="form-control" />
        <span asp-validation-for="Username" class="text-danger"></span>
    </div>

    @* Password input with validation *@
    <div class="form-group">
        <label asp-for="Password"></label>
        <input asp-for="Password" class="form-control" type="password" />
        <span asp-validation-for="Password" class="text-danger"></span>
    </div>

    @* Confirm password input with validation *@
    <div class="form-group">
        <label asp-for="ConfirmPassword"></label>
        <input asp-for="ConfirmPassword" class="form-control" type="password" />
        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
    </div>

    @* Role selection checkboxes *@
    <div class="form-group">
        <label>Roles</label>
        @foreach (var role in Model.Roles)
        {
            <div class="form-check">
                <input type="checkbox" class="form-check-input" name="SelectedRoles" value="@role.Value" />
                <label class="form-check-label">@role.Text</label>
            </div>
        }
    </div>
    <button type="submit" class="btn btn-primary">Create</button>
</form>
